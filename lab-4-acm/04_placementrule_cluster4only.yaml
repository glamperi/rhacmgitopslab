apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  labels:
    app: httpd
  name: cluster4only
  namespace: simple-app
spec:
  clusterSelector:
    matchLabels:
      clusterid: thomas-aks 
